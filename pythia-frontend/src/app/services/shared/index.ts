/**
 * Shared Services - Barrel Export
 *
 * Cross-domain utility services used throughout the application
 * Reusable services that don't belong to a specific business domain
 *
 * Services (Future):
 * - NotificationService - Toast notifications & alerts
 * - LoadingService     - Global loading state
 * - StorageService     - LocalStorage/SessionStorage wrapper
 * - ThemeService       - Theme switching (light/dark mode)
 *
 * Usage:
 * ```typescript
 * import { NotificationService } from '@app/services/shared';
 * ```
 */

// Future shared services will be exported here
