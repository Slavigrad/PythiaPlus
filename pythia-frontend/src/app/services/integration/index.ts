/**
 * Integration Services - Barrel Export
 *
 * External system integrations (export, import, APIs)
 * Services for communicating with external systems and formats
 *
 * Domains:
 * - /export - File export (CSV, JSON, PDF)
 * - /import - File import (future)
 * - /api    - External API integrations (future)
 *
 * Usage:
 * ```typescript
 * import { ExportService } from '@app/services/integration/export';
 * ```
 */

export * from './export';
